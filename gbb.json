{
    "version": "0.4.0",
    "tool": "go install",
    "importpath": "github.com/voidint/swagger-hub/build",
    "variables": [
        {
            "variable": "Date",
            "value": "{{.Date}}"
        },
        {
            "variable": "Commit",
            "value": "{{.GitCommit}}"
        }
    ]
}